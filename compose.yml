version: '3.6'
services:
  caddy:
    image: docker.io/caddy:2.6-alpine
    restart: unless-stopped
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile
    networks:
      - reverseproxy
      - caddy2

networks:
    reverseproxy:
      name: reverseproxy
      external: true
    caddy2:
